#' @param strict A non-NA logical value. Should only *explicit calls*, i.e.
#'   `transltr::translate()`, be extracted? If `FALSE`, calls to *any* such
#'   function are extracted, i.e. `translate()`.
