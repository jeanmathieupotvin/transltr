#' @param strict A non-[NA][base::NA] logical value. Should only
#'   *explicit calls*, i.e. `transltr::translate()`, be extracted? If `FALSE`,
#'   calls to *any* such function are extracted regardless of the underlying
#'   namespace, i.e. `translate()`.
