#' @param throw_error A non-[NA][base::NA] logical value. Should an error be
#'   thrown? If so, [stops()] is called. Otherwise, error messages are returned
#'   as a character vector (possibly empty).
